name: Temporary PR135 final format

on:
  push:
    branches:
      - agent/championship-source-import

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/championship-source-import
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/features/admin/championships/domain/championshipTransactionalImport.ts src/features/admin/championships/pages/AdminChampionshipImportPage.tsx src/features/admin/championships/pages/AdminChampionshipsPage.tsx src/features/admin/championships/services/championshipImportService.ts src/features/admin/championships/services/championshipSourceFileService.ts tests/championshipTransactionalImport.test.mjs src/app/router.tsx src/features/admin/config/adminPermissions.ts src/shared/config/routes.ts tsconfig.test.json
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/features/admin/championships src/app/router.tsx src/features/admin/config/adminPermissions.ts src/shared/config/routes.ts tests/championshipTransactionalImport.test.mjs tsconfig.test.json
          git commit -m "style(championship): format final import workflow"
          git push
