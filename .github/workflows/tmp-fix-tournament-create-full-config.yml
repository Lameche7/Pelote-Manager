name: Temporary fix tournament creation full configuration

on:
  push:
    branches:
      - fix/tournament-create-full-config

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/tournament-create-full-config
          fetch-depth: 0
      - name: Probe
        run: echo ok
